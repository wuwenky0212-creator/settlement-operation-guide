# Nixpacks 配置文件 - 后端服务
[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["alembic upgrade head || echo 'Migration skipped'"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
