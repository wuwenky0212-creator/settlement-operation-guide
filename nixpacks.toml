[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install",
  "cd frontend && npm install"
]

[phases.build]
cmds = [
  "cd frontend && npm run build",
  "ls -la frontend/dist || echo 'Build verification failed'"
]

[start]
cmd = "node server.js"
